[tool:pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    slow: tests that require heavy dependencies or are slower to run
    gpu: tests that require GPU acceleration
    network: tests that require network access
    e2e: end-to-end integration tests
