# =============================================================================
# Setup Configuration for Intrinsical RAG Prototype (legacy fallback)
# =============================================================================
# NOTE: All packaging metadata, entry points and options are defined in pyproject.toml.
# This file is intentionally minimal to avoid duplication.

; (intentionally left blank)

[bdist_wheel]
universal = 0
; egg_info settings not needed; handled by setuptools defaults
